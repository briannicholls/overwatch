console.log('** Loaded heroKeypad copy.js **')

function defer(method) {
    if (document.getElementsByClassName('hero-checkbox').length > 0) {
        method();
    } else {
        setTimeout(function() { defer(method) }, 50);
    }
}

function setImages() {
  let heroCheckboxes = document.getElementsByClassName('hero-checkbox')
  Array.from(heroCheckboxes).forEach( checkbox => {
    debugger
    let heroName = checkbox.innerHTML
    checkbox.style.backgroundImage = "<%= asset_path('ana.jpg') %>"
  })
}

defer(setImages)