<p style="color: green"><%= notice %></p>

<%= link_to 'Manage Hard Counters', '/hard_counters' %>
<%= link_to 'Team Analyzer', '/teams' %>

<h1>Heros</h1>

<div class="row">
  <% @heros.each do |hero| %>
    <%= render hero %>
  <% end %>
</div>
<%= link_to "New hero", new_hero_path %>
